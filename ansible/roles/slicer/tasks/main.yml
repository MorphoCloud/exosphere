---
- name: 'operating system-specific variables imported'
  include_vars: "{{ item }}"
  with_first_found:
    - files:
        - "{{ ansible_distribution }}-{{ ansible_distribution_version }}.yml"
        - "{{ ansible_distribution }}-{{ ansible_distribution_major_version }}.yml"
        - "{{ ansible_distribution }}.yml"
        - "{{ ansible_os_family }}.yml"
      paths:
        - "{{ role_path }}/vars"

# See https://linuxconfig.org/disable-automatic-updates-on-ubuntu-22-04-jammy-jellyfish-linux
- name: Stop service unattended-upgrades
  ansible.builtin.systemd_service:
    name: unattended-upgrades
    state: stopped

- name: Set APT::Periodic::Update-Package-Lists to 0
  ansible.builtin.lineinfile:
    path: /etc/apt/apt.conf.d/20auto-upgrades
    regexp: '^APT::Periodic::Update-Package-Lists'
    line: 'APT::Periodic::Update-Package-Lists "0";'
    backup: yes
  become: true

- name: Set APT::Periodic::Unattended-Upgrade to 0
  ansible.builtin.lineinfile:
    path: /etc/apt/apt.conf.d/20auto-upgrades
    regexp: '^APT::Periodic::Unattended-Upgrade'
    line: 'APT::Periodic::Unattended-Upgrade "0";'
    backup: yes
  become: true

- name: Stop service apt-daily-upgrade.timer
  ansible.builtin.systemd_service:
    name: apt-daily-upgrade.timer
    state: stopped

# See https://help.ubuntu.com/community/Upgrades#Upgrade_policy
- name: Set Prompt to 'never' in /etc/update-manager/release-upgrades
  ansible.builtin.lineinfile:
    path: /etc/update-manager/release-upgrades
    regexp: '^Prompt=.*'
    line: 'Prompt=never'
    backup: yes
    state: present
  become: true

- name: Get process IDs of 'check-new-release-gtk'
  community.general.pids:
    pattern: "/usr/lib/ubuntu-release-upgrader/check-new-release-gtk"
  register: release_upgrader_pids

- name: Print the 'check-new-release-gtk' process IDs
  ansible.builtin.debug:
    msg: "PIDs of check-new-release-gtk: {{ release_upgrader_pids.pids | join(', ') }}"
  when: release_upgrader_pids.pids | length > 0

- name: Kill 'check-new-release-gtk' processes
  ansible.builtin.command:
    cmd: "kill -s SIGKILL {{ item }}"
  loop: "{{ release_upgrader_pids.pids }}"
  when: release_upgrader_pids.pids | length > 0

- name: 'Slicer prerequisites installed'
  package:
    name: '{{ item }}'
    state: 'present'
  loop: '{{ slicer_prerequisites_package_names }}'

- name: 'Slicer install directory created'
  ansible.builtin.file:
    path: "{{ slicer_install_dir }}"
    state: directory
    owner: "exouser"
    group: "exouser"

- name: 'dist directory created'
  ansible.builtin.file:
    path: "{{ support_install_dir }}/dist"
    state: directory
    owner: "exouser"
    group: "exouser"

#- name: 'helper script downloaded'
#  get_url:
#    url: "https://raw.githubusercontent.com/Slicer/SlicerDocker/e9353bd4e1b861c5fb78ac4cd90553696b119d22/scripts/slicer-download.sh"
#    dest: "{{ support_install_dir }}/dist/slicer-download.sh"
#    mode: "744"
#    owner: "exouser"
#    group: "exouser"

- name: 'File "slicer-download.sh" copied'
  copy:
    src: 'slicer-download.sh'
    dest: "{{ support_install_dir }}/dist/slicer-download.sh"
    mode: "744"
    owner: "exouser"
    group: "exouser"

- name: 'Slicer application installed'
  become: true
  become_user: exouser
  command: "{{ support_install_dir }}/dist/slicer-download.sh -d {{ slicer_install_dir }}/Slicer -r {{ slicer_release_id }} -s"
  args:
    creates: "{{ slicer_install_dir }}/Slicer"

- name: 'File "Slicer.desktop" installed as panel launcher'
  copy:
    src: 'Slicer.desktop'
    dest: '/usr/share/applications/Slicer.desktop'
    owner: "exouser"
    group: "exouser"
  when: 'gui_enabled is defined and gui_enabled == true'

- name: 'Placeholder "Slicer-X.Y" updated in file "Slicer.desktop"'
  ansible.builtin.replace:
    path: "/usr/share/applications/Slicer.desktop"
    regexp: 'Slicer\-X\.Y'
    replace: 'Slicer-{{ slicer_version["major"] }}.{{ slicer_version["minor"] }}'
  when: 'gui_enabled is defined and gui_enabled == true'

- name: 'Check if system has at least one GPU'
  ansible.builtin.command: nvidia-detector
  register: nvidia_detector_cmd

- name: 'Exec command in file "Slicer.desktop" updated to use "vglrun -d egl" wrapper'
  ansible.builtin.replace:
    path: "/usr/share/applications/Slicer.desktop"
    regexp: 'SLICER\_INSTALL\_DIR\/Slicer\/Slicer'
    replace: 'vglrun -d egl SLICER_INSTALL_DIR/Slicer/Slicer'
  when: 'nvidia_detector_cmd.stdout != "None" and gui_enabled is defined and gui_enabled == true'

- name: 'Placeholder "SLICER_INSTALL_DIR" updated in file "Slicer.desktop"'
  ansible.builtin.replace:
    path: "/usr/share/applications/Slicer.desktop"
    regexp: 'SLICER\_INSTALL\_DIR'
    replace: '{{ slicer_install_dir }}'
  when: 'gui_enabled is defined and gui_enabled == true'

- name: 'File "Slicer.desktop" installed as Desktop launcher'
  copy:
    src: '/usr/share/applications/Slicer.desktop'
    dest: '/home/exouser/Desktop/Slicer.desktop'
    owner: "exouser"
    group: "exouser"
  when: 'gui_enabled is defined and gui_enabled == true'

- name: 'File "slicer-update-settings.py" copied'
  copy:
    src: 'slicer-update-settings.py'
    dest: "{{ support_install_dir }}/dist/slicer-update-settings.py"
    mode: "644"
    owner: "exouser"
    group: "exouser"

- name: 'Slicer update settings'
  become: true
  become_user: exouser
  command: "{{ slicer_install_dir }}/Slicer/Slicer --disable-modules --python-script {{ support_install_dir }}/dist/slicer-update-settings.py"
  environment:
    DISPLAY: :1.0

- name: 'File "slicer-install-extension.py" copied'
  copy:
    src: 'slicer-install-extension.py'
    dest: "{{ support_install_dir }}/dist/slicer-install-extension.py"
    mode: "644"
    owner: "exouser"
    group: "exouser"

- name: 'Slicer extensions installed'
  become: true
  become_user: exouser
  command: "{{ slicer_install_dir }}/Slicer/Slicer --disable-modules --python-script {{ support_install_dir }}/dist/slicer-install-extension.py {{ item }}"
  loop: '{{ slicer_extensions }}'
  environment:
    DISPLAY: :1.0

- name: 'File "slicer-install-extension-dependencies.py" copied'
  copy:
    src: 'slicer-install-extension-dependencies.py'
    dest: "{{ support_install_dir }}/dist/slicer-install-extension-dependencies.py"
    mode: "644"
    owner: "exouser"
    group: "exouser"

- name: 'Slicer extension dependencies installed'
  become: true
  become_user: exouser
  command: "{{ slicer_install_dir }}/Slicer/Slicer --python-script {{ support_install_dir }}/dist/slicer-install-extension-dependencies.py"
  environment:
    DISPLAY: :1.0

- name: 'File "husky_small.png" downloaded'
  get_url:
    url: "https://raw.githubusercontent.com/SlicerMorph/Images/main/husky_small.png"
    dest: "/home/exouser/Downloads/husky_small.png"
    mode: "644"
    owner: "exouser"
    group: "exouser"
  when: 'gui_enabled is defined and gui_enabled == true'

# See https://github.com/jamescherti/watch-xfce-xfconf
- name: 'Theme name updated to "Yaru-magenta"'
  become: true
  become_user: exouser
  xfconf:
    channel: "xsettings"
    property: "/Net/ThemeName"
    value_type: "string"
    value: "Yaru-magenta"
  environment:
    XAUTHORITY: /home/exouser/.Xauthority
    DISPLAY: :1.0

- name: 'Icon theme name updated to "Yaru-magenta"'
  become: true
  become_user: exouser
  xfconf:
    channel: "xsettings"
    property: "/Net/IconThemeName"
    value_type: "string"
    value: "Yaru-magenta"
  environment:
    XAUTHORITY: /home/exouser/.Xauthority
    DISPLAY: :1.0

- name: 'Desktop background updated'
  become: true
  become_user: exouser
  xfconf:
    channel: "xfce4-desktop"
    property: "/backdrop/screen0/monitorVNC-0/workspace0/last-image"
    value_type: "string"
    value: "/home/exouser/Downloads/husky_small.png"
  environment:
    XAUTHORITY: /home/exouser/.Xauthority
    DISPLAY: :1.0

- name: 'Desktop background centered'
  become: true
  become_user: exouser
  xfconf:
    channel: "xfce4-desktop"
    property: "/backdrop/screen0/monitorVNC-0/workspace0/image-style"
    value_type: "int"
    value: "1"
  environment:
    XAUTHORITY: /home/exouser/.Xauthority
    DISPLAY: :1.0

- name: 'Check if file "/home/exouser/.config/xfce4/panel-configured" exists'
  stat:
    path: "/home/exouser/.config/xfce4/panel-configured"
  register: panel_configured

- name: 'Script "update-xfce4-panel.sh" installed'
  copy:
    src: 'update-xfce4-panel.sh'
    dest: '/home/exouser/.config/xfce4/update-xfce4-panel.sh'
    mode: "744"
    owner: "exouser"
    group: "exouser"
  when: 'gui_enabled is defined and gui_enabled == true'

- name: 'xfce4-panel settings updated'
  become: true
  become_user: exouser
  ansible.builtin.script: |
    /home/exouser/.config/xfce4/update-xfce4-panel.sh
    # Create a marker file to indicate that the panel has been configured
    touch /home/exouser/.config/xfce4/panel-configured
  environment:
    XAUTHORITY: /home/exouser/.Xauthority
    DISPLAY: :1.0
  when: 'not panel_configured.stat.exists and gui_enabled is defined and gui_enabled == true'

# Adapted from https://github.com/cli/cli/blob/trunk/docs/install_linux.md#debian-ubuntu-linux-raspberry-pi-os-apt
- name: 'GitHub Client GPG key added'
  get_url:
    url: 'https://cli.github.com/packages/githubcli-archive-keyring.gpg'
    dest: '/etc/apt/keyrings/githubcli-archive-keyring.gpg'
    mode: '0644'

- name: 'GitHub Client repository added'
  apt_repository:
    repo: 'deb [arch=amd64 signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main'
    state: 'present'
    filename: 'github-cli'
    update_cache: 'true'

- name: 'GitHub Client package installed'
  apt:
    name: 'gh'
    state: 'present'

- name: 'File "check_instance_shelve.sh" copied'
  copy:
    src: 'check-instance-shelve.sh'
    dest: "{{ support_install_dir }}/dist/check-instance-shelve.sh"
    mode: "755"
    owner: "exouser"
    group: "exouser"

- name: 'Added @reboot crontab entry for checking instance shelving'
  ansible.builtin.cron:
    name: "Touch shelving instance tracker file"
    user: exouser
    special_time: reboot
    job: "{{ support_install_dir }}/dist/check-instance-shelve.sh"

- name: 'Shelving instance tracker file touched'
  become: true
  become_user: exouser
  command: "{{ support_install_dir }}/dist/check-instance-shelve.sh"

- name: 'Added crontab for checking for instance shelving extension'
  cron:
    name: 'Ask user for instance shelving extension'
    user: exouser
    minute: '*/5'
    job: "{{ support_install_dir }}/dist/check-instance-shelve.sh -a"